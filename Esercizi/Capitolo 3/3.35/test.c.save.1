
#include <stdio.h>

#define TRUE 1
#define FALSE 0
typedef int BOOL; 

int main(int argc, char const *argv[])
{
	
	int num = 1234;
	int dec = 1000;
	int unit;

	int arr[4];

	printf("NUMERO INIZIALE : %d\n",  num);
	

	int cont = 0x0;
	while(dec){
		unit = num / dec;
		arr[cont] =  unit;
		cont++;
		num %= dec;
		dec /= 10;
	}


	for(int i = 0; i < sizeof(arr) / 4; i++){
	
		printf("%d\n", arr[i]);
	
	}

	return 0;

	BOOL
 pari = FALSE; BOOL dispari = FALSE;
	float grandezza = sizeof(arr) / 2;
	
	(grandezza % 2 == 0) ? pari = true : dispari = true;

	(pari == true) ? printf("L'array è pari") : printf("L'array è dispari");

return 0; // Fine del programma

}
